apiVersion: v1
kind: Service
metadata:
  name: udagram-api-user
  labels:
    run: udagram-api-user
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: user-port
  type: ClusterIP
  selector:
    run: udagram-api-user